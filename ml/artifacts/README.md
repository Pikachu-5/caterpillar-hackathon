# Local artifacts

Run `uv run python train_models.py --data ../data/generated --artifacts artifacts` from `ml/` to create the ML-001 ETA model and metadata/evaluation report here. Artifacts remain local and gitignored; regenerate them from the checked-in dataset and pinned Python dependencies.
